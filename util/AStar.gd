#class_name AStar


#var MapTile = load("res://util/MapTile.gd");
# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
#var tileQueue = [];
##pass in an a 2d array of MapTile classes
#static func BuildPath(grid,xStart,zStart,xEnd,zEnd):
	#print("grid size " + String(grid.size()));
	
	#tileQueue.push_back(grid[xEnd][zEnd]);
	#var currentTile;
	#while(currentTile = tileQueue.pop_front()):
		
